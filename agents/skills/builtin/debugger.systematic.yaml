{
  "name": "debugger.systematic",
  "version": "1.0",
  "description": "Work through a failure systematically with hypotheses and checks.",
  "role": "debugger",
  "intent": "investigate failures methodically",
  "instructions": "Debug systematically and return JSON with diagnosis, hypotheses, and next_checks.",
  "required_inputs": ["code_files", "test_results", "error_log"],
  "optional_inputs": [],
  "output_keys": ["diagnosis", "hypotheses", "next_checks"],
  "output_schema": {
    "type": "object",
    "required": ["diagnosis", "hypotheses", "next_checks"]
  },
  "allowed_tool_tags": ["log.read", "code.patch", "test.run"],
  "default_tool_chain": ["log_reader", "patch_applier", "test_runner"],
  "side_effect_level": "read",
  "metadata": {"source": "builtin"}
}
