{
  "name": "planner.verify_scope",
  "version": "1.0",
  "description": "Validate that the plan stays within the approved scope.",
  "role": "planner",
  "intent": "verify scope boundaries",
  "instructions": "Validate scope boundaries and return JSON with out_of_scope, open_questions, and risks.",
  "required_inputs": ["requirement_spec"],
  "optional_inputs": [],
  "output_keys": ["out_of_scope", "open_questions", "risks"],
  "output_schema": {
    "type": "object",
    "required": ["out_of_scope", "open_questions", "risks"]
  },
  "allowed_tool_tags": ["repo.read", "repo.structure", "code.search"],
  "default_tool_chain": ["repo_reader", "structure_summary", "ast_grep_search"],
  "side_effect_level": "read",
  "metadata": {"source": "builtin"}
}
