{
  "name": "pm.specify",
  "version": "1.0",
  "description": "Turn a requirement into a more formal spec snapshot.",
  "role": "pm",
  "intent": "formalize requirement scope",
  "instructions": "Turn the request into a spec snapshot and return JSON with summary, acceptance_criteria, and constraints.",
  "required_inputs": ["requirement", "conversation"],
  "optional_inputs": [],
  "output_keys": ["summary", "acceptance_criteria", "constraints"],
  "output_schema": {
    "type": "object",
    "required": ["summary", "acceptance_criteria", "constraints"]
  },
  "allowed_tool_tags": ["repo.read", "repo.search", "code.parse"],
  "default_tool_chain": ["repo_reader", "rg_search", "py_tree_sitter_parse"],
  "side_effect_level": "read",
  "metadata": {"source": "builtin"}
}
