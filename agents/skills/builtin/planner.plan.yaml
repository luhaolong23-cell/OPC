{
  "name": "planner.plan",
  "version": "1.0",
  "description": "Turn an approved requirement spec into a task plan.",
  "role": "planner",
  "intent": "produce executable plans",
  "instructions": "Turn the requirement spec into JSON with summary, tasks, and risks.",
  "required_inputs": ["requirement_spec"],
  "optional_inputs": [],
  "output_keys": ["summary", "tasks", "risks"],
  "output_schema": {
    "type": "object",
    "required": ["summary", "tasks", "risks"]
  },
  "allowed_tool_tags": ["repo.read", "repo.structure", "code.search"],
  "default_tool_chain": ["repo_reader", "structure_summary", "ast_grep_search"],
  "side_effect_level": "read",
  "metadata": {"source": "builtin"}
}
