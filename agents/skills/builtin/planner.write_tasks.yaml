{
  "name": "planner.write_tasks",
  "version": "1.0",
  "description": "Expand a requirement spec into a detailed task checklist.",
  "role": "planner",
  "intent": "expand execution tasks",
  "instructions": "Expand the requirement spec into an execution checklist and return JSON with tasks, dependencies, and milestones.",
  "required_inputs": ["requirement_spec"],
  "optional_inputs": [],
  "output_keys": ["tasks", "dependencies", "milestones"],
  "output_schema": {
    "type": "object",
    "required": ["tasks", "dependencies", "milestones"]
  },
  "allowed_tool_tags": ["repo.read", "repo.structure", "code.search"],
  "default_tool_chain": ["repo_reader", "structure_summary", "ast_grep_search"],
  "side_effect_level": "read",
  "metadata": {"source": "builtin"}
}
