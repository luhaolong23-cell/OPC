{
  "name": "reviewer.audit",
  "version": "1.0",
  "description": "Audit the proposed change set before completion.",
  "role": "reviewer",
  "intent": "perform final quality audit",
  "instructions": "Review the change and return JSON with approved, issues, risk_level, and summary.",
  "required_inputs": ["plan", "code_files", "test_results"],
  "optional_inputs": [],
  "output_keys": ["approved", "issues", "risk_level", "summary"],
  "output_schema": {
    "type": "object",
    "required": ["approved", "issues", "risk_level", "summary"]
  },
  "allowed_tool_tags": ["diff.read", "security.scan", "diff.syntax"],
  "default_tool_chain": ["diff_reader", "semgrep_scan", "difftastic_diff"],
  "side_effect_level": "read",
  "metadata": {"source": "builtin"}
}
